# view base.rb.erb

def self.call(menv)
  super
  path = menv["REQUEST_PATH"]

  self.response_status = 200
  self.response_headers = {
    "Framework" => "Liza \#{Lizarb::VERSION}"
  }
  self.response_body = "It works! <%= controller_name.camelize %>Request"
end
