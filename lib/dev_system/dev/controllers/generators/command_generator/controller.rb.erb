class <%= @name.camelize %>Command < Liza::Command

  def self.call args
    log "args = #{args.inspect}"
    new.call args
  end

  def call args
    log "@args = #{args.inspect}"
    @args = args

    # log "puts render :inline, format: :txt"
    # puts render :inline, format: :txt

    # log "puts render :text, format: :txt"
    # puts render :text, format: :txt

    log "Time.now #{Time.now}"
  end

end

__END__

The default view can be rendered by calling `render :inline, format: :txt`

Self is the instance of the class that called `render`:
  self: <%%= self %>

# view text.txt.erb

This named view can be rendered by calling `render :text, format: :txt`

Instance variables are accessible from the view:
  @args: <%%= @args %>

Written at <%%= Time.now %>

# view lalala.txt.erb.ignored

This named view will be ignored.
