# view section_panel.rb.erb

  define_error(:not_found) do |args|
    "<%= subsystem_name %> not found: #{args[0].inspect}"
  end

  def call(other_menv)
    log "not implemented"
  #   log "forging"
  #   menv = { other_menv: }

  #   log "finding"
  #   k = Liza.const "#{other_menv[:<%= subsystem_name %>]}_<%= subsystem_name %>"
    
  #   log "forwarding"
  #   k.call menv
  # rescue Liza::ConstNotFound
  #   raise_error :not_found, string
  end
